import type { Application, RequestHandler } from 'express';
import type { Class } from 'n8n-core';
import type { BooleanLicenseFeature } from '../Interfaces';
import type { RouteScopeMetadata } from './types';
export declare const createLicenseMiddleware: (features: BooleanLicenseFeature[]) => RequestHandler;
export declare const createScopedMiddleware: (routeScopeMetadata: RouteScopeMetadata[string]) => RequestHandler;
export declare const registerController: (app: Application, controllerClass: Class<object>) => void;
