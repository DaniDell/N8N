import { UsageMetricsRepository } from '../databases/repositories/usageMetrics.repository';
export declare class UsageMetricsService {
    private readonly usageMetricsRepository;
    constructor(usageMetricsRepository: UsageMetricsRepository);
    collectUsageMetrics(): Promise<{
        name: string;
        value: number;
    }[]>;
}
